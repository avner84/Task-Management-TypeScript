const port: number = 8080;
const protocol: string = "http";
const host: string = "localhost";
const origin: string = `http://localhost:3000`; // CLIENT_URL
const api: string = `http://localhost:8080`; // API_URL

export { port, protocol, host, origin, api };
